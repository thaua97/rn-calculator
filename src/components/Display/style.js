import { StyleSheet } from 'react-native'

const Styles = StyleSheet.create({
    display: {
        flex: 1,
        padding: 20,
        justifyContent: 'center',
        backgroundColor: 'rgb(20, 4, 56)',
        alignItems: 'flex-end',
    },
    display__value: {
        fontSize: 60,
        color: '#fff',
    }
})

export default Styles